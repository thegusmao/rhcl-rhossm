# L2Advertisement — anuncia IPs do edge-pool via ARP/NDP na rede local
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: rhcl-l2
  namespace: metallb-system
spec:
  ipAddressPools:
    - rhcl-pool
